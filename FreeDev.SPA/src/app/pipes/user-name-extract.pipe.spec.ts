import { UserNameExtractPipe } from './user-name-extract.pipe';

describe('UserNameExtractPipe', () => {
  it('create an instance', () => {
    const pipe = new UserNameExtractPipe();
    expect(pipe).toBeTruthy();
  });
});
